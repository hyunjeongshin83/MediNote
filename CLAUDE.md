# 이 저장소에서 일할 때

숙명여대 약학대학 방준석 교수님 연구실 — 복약관리 + 생애주기 예방접종.
고령친화 헬스케어(웹 · 안드로이드 · iOS).

---

## 1. 조사하는 쪽과 고치는 쪽이 다릅니다 ★

```
조사   예약 리서치  →  GitHub 이슈에 「고칠 것」을 적는다.  코드는 안 고친다.
반영   Claude Code  →  그 이슈를 읽고 실제로 고친다.
```

사이에 사람이 없습니다. 그래서 형식이 정해져 있습니다.

**반영하러 왔다면 먼저 이것부터 보세요.**

```
docs/FIXES.md        지금 무엇이 바로 반영 가능한지 (사람이 읽는 표)
docs/FIX-FORMAT.md   「고칠 것」을 적는 형식
tools/fixes.py       이슈에서 직접 읽어 내는 도구
```

`docs/FIXES.md` 의 **「바로 반영해도 되는 것」** 절에 있는 것만 확인 없이 고칩니다.
**「사람이 정해야 넘어가는 것」은 손대지 마세요** — 막는 것이 무엇인지 그 표에 적혀 있습니다.

**고치고 나면 두 가지를 하세요.**

1. 그 이슈의 `fix` 칸에서 `상태:` 를 `반영됨 · <커밋해시>` 로 바꿉니다
2. `python3 tools/fixes.py --report` 로 `docs/FIXES.md` 를 다시 만들어 함께 커밋합니다

이걸 안 하면 다음 번에 같은 것을 또 고칩니다.

**조사하러 왔다면** — 코드를 고치지 말고 `docs/FIX-FORMAT.md` 의 형식으로 이슈에만
적으세요. 사람이 정해야 할 것이 하나라도 있으면 `막는것` 을 반드시 채웁니다.
비워 두면 확인 없이 고쳐집니다.

### 가로지르는 것은 공용 창구로

여러 앱에 똑같이 걸리는 것(규제·인증·LLM·접근성·검사 도구)은
`hyunjeongshin83/QA-tool` 에도 올립니다. 규약은 그 저장소 이슈 #1 입니다.

```
QA-tool 에만   그 앱 관리자가 몰라도 되는 것 — 기법·기준·도구 자체
양쪽에         이 앱의 코드를 실제로 고쳐야 하는 것
               본문 끝에 짝지음표 <!-- qa-link: QA-N --> 를 달아 짝을 맞춥니다
여기에만       이 앱 안에서만 뜻이 있는 것
```

**`fix` 칸은 QA-tool 이 아니라 여기 답니다** — 고치는 도구가 이 저장소에서 돕니다.
QA-tool 에는 무엇을 알아냈는지만 적습니다.

**QA-tool 은 공개 저장소입니다.** 열쇠·토큰 위치, 계정·PIN, 아직 안 밝힌 일정은
올리지 않습니다.

## 2. 확인 없이 바꾸지 않는 것

```
· 눈에 띄는 문구·디자인 변경          UX-RULES R16
· 임상 판정 로직 · 접종 권장·금기 문구  질병관리청 지침·IRB 문서에 맞춰 둔 것입니다
· 건강 데이터가 어디에 저장되는지      동의·법적 검토가 걸립니다
· 스토어 등록 설명·홍보 문구           식약처는 표시된 사용목적으로 의료기기를 가릅니다 (#15)
```

마지막 줄이 특히 그렇습니다. **코드보다 카피가 먼저 걸립니다.**
「부작용 모니터링」·「위험 알람」·「진단」·「비침습 연속 측정」은 한 줄만 써도 판정이 뒤집힙니다.

## 3. 건강 데이터가 실제로 어디로 가는지

문서가 사실과 다르게 적혀 있던 적이 있습니다. 지금 확인된 것은 이렇습니다.

```
블루투스(BLE) 측정값   사용자가 「읽은 값 클라우드에 저장」을 눌러야 올라갑니다
                       → Supabase measurements (도쿄 리전)
질환(conditions)        프로필 저장 시 올라갑니다 (이름·생년·알레르기·약 등도 같은 표)
                       → Supabase health_profiles
증상 기록               저장 단추를 누르면 올라갑니다 + 기기 localStorage 에도 사본
                       → Supabase hub_state 키 medinote:report:u_<uid> (비로그인은 g_<난수>)
AI 도우미 · AI 채움     묻는 순간 나갑니다 — 프로필(이름·나이대·질환) + 대화 + 최근 증상 8건 + 복용약 20개
                       → Supabase 엣지 함수 ai-helper → api.anthropic.com (미국). 서버에는 토큰 수만 남습니다
생년월일               Google 로그인 때 Google People API 에서 받아 와 health_profiles 로
Health Connect (안드로이드 앱만)  「건강기기 연결」 시트의 「건강 데이터 읽기」 단추로 걸음·심박·수면을 읽습니다
                       → 기기 localStorage medinote:hc:v1 에만. 서버로 보내지 않습니다 (MN-14-2, 2026-09-25)
HealthKit (iOS)        매니저는 있으나 어느 화면에서도 불리지 않습니다
```

세 줄(증상 기록 · AI 도우미 · 생년월일)은 2026-09-24 에 확인해 더한 것입니다 (#22).
**AI 도우미 경로는 개인정보처리방침(PRIVACY.md)에 아직 없습니다** — MN-22-2.

**「서버로 보내지 않습니다」·「기기를 떠나지 않습니다」라고 쓰지 마세요.** 사실이 아닙니다.

## 4. 앱 화면 세 벌

`MediNote_apps_0706/*/index.html` 은 `MediNote_app.html` 에서 만들어 냅니다.
손으로 옮기면 갈라집니다 — 실제로 두 달 넘게 갈라져 있었습니다 (#12).

```
python3 tools/build-packaged-app.py            다시 만듭니다
python3 tools/build-packaged-app.py --check    갈라졌는지만 봅니다 (다르면 1)
```

APK 빌드가 매번 `--check` 를 먼저 돌립니다. 갈라진 채로 밀면 CI 가 빨개집니다.

## 5. APK

Actions 탭 → 「안드로이드 APK」 → Artifacts 의 `medinote-debug-N`.
**디버그 빌드**입니다. 스토어 등록에는 서명한 릴리스 빌드가 따로 필요합니다.

## 6. 화면을 고칠 때

백신노트·메디노트·금연앱이 한 팀이 만든 것처럼 보여야 합니다.
새 색·새 글자 크기를 만들지 마세요. 색 대비를 바꿨으면 WCAG 상대휘도로 계산해
수치를 남기세요 — 눈대중 금지.

## 7. 비밀값

열쇠·연결 문자열을 이슈·커밋·카톡에 넣지 마세요.
참여자·건강 데이터를 이슈에 옮겨 적지 마세요. 필요하면 분포와 개수만 씁니다.
