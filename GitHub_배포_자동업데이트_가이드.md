# MediNote 배포 · 링크 공유 · 자동 업데이트 가이드

목표: **링크 하나로** 교수님이 열어 실사용하고, 코드가 바뀌면 **자동으로 갱신**되게 하기.

---

## A. 링크만으로 보이게 하는 원리
- 웹앱은 서버(호스팅)에 올려야 링크가 생깁니다. 파일을 카톡/메일로 보내면 링크가 아니라 파일입니다.
- 링크의 "루트 주소"는 `index.html`을 엽니다. 그래서 이 묶음에 **`index.html`을 넣어**, 루트 링크를 누르면 곧바로 `MediNote_app.html`(앱)이 열리게 해두었습니다.
- 예: `https://아이디.github.io/medinote/` → 자동으로 앱이 뜸.

## B. 가장 빠른 방법 (git 몰라도 됨) — Netlify Drop
1. https://app.netlify.com/drop 접속
2. 이 배포 폴더(zip 푼 것)를 **통째로 드래그&드롭**
3. 몇 초 뒤 `https://랜덤이름.netlify.app` **링크 생성** → 교수님께 그대로 전달
4. 수정본이 나오면 같은 사이트에 다시 드롭하면 갱신(수동). 자동까지 원하면 아래 C(GitHub 연결).

## C. GitHub Pages — 안정 주소 + 자동 업데이트
1. github.com에서 새 저장소(repo) 생성 (예: `medinote`)
2. 이 폴더 파일 전체 업로드(웹에서 "Add file → Upload files"로 드래그 가능)
3. **Settings → Pages → Branch: main / (root) → Save**
4. 1~2분 뒤 `https://아이디.github.io/medinote/` 링크 생성 (index.html 덕분에 앱이 바로 열림)
5. **자동 업데이트**: 이후 저장소에 파일을 push(또는 웹에서 다시 업로드)하면 **Pages가 자동으로 재배포**됩니다(약 1분). 별도 서버 작업 불필요.

## D. "토큰이 있으면 자동 업데이트 되나요?" — 네, 됩니다
- 자동 업데이트는 두 단계입니다: ① 바뀐 파일을 저장소에 올리기(push) ② 저장소→사이트는 GitHub Pages가 **자동** 재배포.
- ②는 이미 자동입니다. 관건은 ①(push)인데, 여기서 **GitHub 토큰**이 필요합니다.
  - 방법 1) **GitHub Desktop 앱**: 클릭 몇 번으로 push(토큰을 직접 다룰 필요 없음). 가장 쉬움.
  - 방법 2) **개인 액세스 토큰(PAT)**: 명령줄/자동화로 push. Fine-grained 토큰을 그 저장소 **Contents: Read/Write**로만 발급해 쓰고, 끝나면 폐기 권장.
  - 방법 3) **GitHub 커넥터 연결**: 앞으로 제가 여기서 직접 파일을 저장소에 올려 갱신할 수 있습니다(토큰을 채팅에 붙일 필요 없이 안전). 이걸 원하시면 GitHub 연결만 해주세요.
- 정리: 저장소만 있으면 push할 때마다 사이트가 자동 갱신됩니다. 제가 대신 push하길 원하시면 GitHub 커넥터를 연결하시는 게 가장 안전합니다.

---

## 배포 후 실사용 체크(이미 준비된 것)
- Supabase 프로젝트 살아있음, `hub_state` 테이블·정책 준비됨 → **클라우드 저장 바로 작동**.
- 예시 안내카드 6개 DB에 넣어둠(`medinote:cards`).
- 로그인: 본인 아이디로 바로 테스트 가능(Google은 Supabase 공급자 설정 후).
- AI 리포트/도우미: 앱에서 Claude 키 1회 입력 시 작동(미입력 시 자동 정리본).

## 주의
- 반드시 **https 주소**에서 여세요(설치·위치·클립보드·로그인 모두 https 필요). Netlify/GitHub Pages는 자동 https입니다.
- 현재 DB는 데모용으로 열려 있어(익명 쓰기 허용) 민감한 실명·상세 증상은 많이 넣지 마세요. 실서비스는 로그인 인증 기반으로 조여야 합니다.
