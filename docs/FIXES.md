# 고칠 것 — 지금 상태

`tools/fixes.py --report` 가 만든 것입니다. **손으로 고치지 마세요** —
고칠 것은 이슈 본문의 `fix` 칸입니다 (형식은 `docs/FIX-FORMAT.md`).

```
저장소       hyunjeongshin83/MediNote
바로 반영    0 건
사람 대기    8 건
반영됨       4 건
보류         0 건
```

## 바로 반영해도 되는 것

`막는것: 없음` 이고 아직 `대기` 인 것입니다. **사람 확인 없이 고쳐도 됩니다.**

없습니다.

## 사람이 정해야 넘어가는 것

| id | 이슈 | 무엇 | 막는 것 |
|---|---|---|---|
| `MN-12-4` | #12 | 살아 있는 빌드 경로인지 7월 보관본인지 불분명합니다 | **살아 있는 경로인지 대표님이 알려주셔야 합니다** |
| `MN-14-1` | #14 | 「Health Connect·Wear OS」라고 적혀 있는데 실제로 도는 것은 블루투스 직접 읽기이고, 305줄은 저장 위치도 틀립니다 | **눈에 띄는 문구 변경이라 대표님 확인이 먼저입니다 (R16). MN-14-2 와 한 몸입니다** |
| `MN-14-2` | #14 | 두 건강 매니저가 잘 짜여 있는데 자바스크립트 다리가 없어 어느 화면에서도 불리지 않습니다 | **Health Connect 를 실제로 붙일지 대표님이 정하셔야 합니다** |
| `MN-14-4` | #14 | 개인정보처리방침이 없었습니다. 건강 데이터를 읽는 앱은 두 스토어 모두 방침 URL 을 요구합니다 | **개인정보 보호책임자 이름과 연락처를 대표님이 알려주셔야 합니다** |
| `MN-15-1` | #15 | hwTipGoogle 이 안 도는 기능을 설명하고 저장 위치도 틀립니다. 식약처는 표시된 사용목적으로 의료기기를 가릅니다 | **MN-14-1 과 같은 결정 · R16 문구 확인** |
| `MN-15-2` | #15 | 「기기 안에만 저장되며」의 범위가 모호합니다. 질환(conditions)은 health_profiles 로 서버에 올라갑니다 | **동의 관련 문구라 대표님 확인이 먼저입니다 (R16)** |
| `MN-15-3` | #15 | 「부작용 모니터링」·「위험 알람」·「비침습 연속 측정」은 한 줄만 써도 의료기기로 판정될 수 있습니다 | **스토어 설명 문안이 아직 없습니다** |
| `MN-15-4` | #15 | 출시 전 「의료기기 해당여부 검토」를 신청하지 않으면 나중에 판정될 때 스토어에서 내려야 합니다 | **신청할지 대표님이 정하셔야 합니다. 코드가 아니라 절차입니다** |

## 이미 반영된 것

| id | 이슈 | 무엇 | 커밋 |
|---|---|---|---|
| `MN-12-1` | #12 | 패키징 사본 셋이 웹앱과 두 달 넘게 갈라져 로그인·클라우드·증상기록 시트가 통째로 없습니다 | `393da5b` |
| `MN-12-2` | #12 | APK_빌드안내.md 가 있다고 적어 둔 워크플로가 저장소에 없습니다 | `ad2222c` |
| `MN-12-3` | #12 | 웹앱→패키징 복사를 손으로 하면 또 갈라집니다 | `ad2222c` |
| `MN-14-3` | #14 | 빌드가 세 군데서 막혀 있었습니다 — 래퍼 없음 · mipmap 없음 · health-connect 의존성 없음 | `ad2222c` |

---

반영한 뒤에는 **이슈의 `fix` 칸에서 `상태` 를 `반영됨 · <커밋>` 으로 바꾸고**,
이 파일을 `--report` 로 다시 만들어 주세요. 그래야 다음에 또 고치지 않습니다.
