# 고칠 것 — 지금 상태

`tools/fixes.py --report` 가 만든 것입니다. **손으로 고치지 마세요** —
고칠 것은 이슈 본문의 `fix` 칸입니다 (형식은 `docs/FIX-FORMAT.md`).

```
저장소       hyunjeongshin83/MediNote
바로 반영    1 건
사람 대기    27 건
반영됨       11 건
보류         0 건
```

## 바로 반영해도 되는 것

`막는것: 없음` 이고 아직 `대기` 인 것입니다. **사람 확인 없이 고쳐도 됩니다.**

### `MN-18-1` — 이슈 #18

| | |
|---|---|
| 파일 | `MediNote_app.html:961, MediNote_app.html:966` |
| 무엇 | 저장 시 판독 전부에 저장 시각 하나(now)를 effective_time 으로 넣어, 판독마다 있던 ts 가 버려집니다 |
| 어떻게 | out.push 에서 effective_time 을 new Date(r.ts).toISOString() 으로, client_ts 는 now 그대로. 한 세션 안 판독이 각자 받은 시각을 갖게 |
| 확인 | 혈압·심박을 시차를 두고 읽은 뒤 저장 → measurements 두 행의 effective_time 이 다름. 화면 문구 변화 없음 |

## 사람이 정해야 넘어가는 것

| id | 이슈 | 무엇 | 막는 것 |
|---|---|---|---|
| `MN-22-2` | #22 | 처리위탁 표에 AI 도우미 사업자(Anthropic PBC · 미국)가 없고, 국외이전 항목이 일본만 적고 있습니다. 민감정보(증상·질환·복용약)가 갑니다 | **개인정보처리방침은 법적 문서입니다. 대표님 확인과 법률 검토가 먼저입니다 (CLAUDE.md §2 「건강 데이터가 어디에 저장되는지」)** |
| `MN-22-3` | #22 | 증상·복용약·질환이 외부 AI 사업자에게 전달된다는 안내가 화면에 없습니다 | **손님이 보는 문구이고 동의 문구입니다 (UX-RULES R16 · CLAUDE.md §2). 「저장되지 않습니다」는 Anthropic 쪽 보관 정책을 확인한 뒤에만 쓸 수 있습니다** |
| `MN-22-4` | #22 | QR 약봉투 히스토리 키 'medinote:rx' · 'medit:rx' 가 공개 읽기 목록에 있습니다. 행은 아직 없지만 쓰는 순간 전원에게 열립니다 | **건강 데이터 저장 위치·형식 변경입니다 (CLAUDE.md §2). MedIT 쪽이 같은 키를 쓰는지 대표님이 확인해 주셔야 합니다** |
| `MN-22-5` | #22 | health_profiles 등 7개 건강 테이블에 anon 이 TRUNCATE · REFERENCES · TRIGGER 권한을 갖고 있습니다. RLS 는 TRUNCATE 를 막지 못합니다 | **운영 DB 권한 변경입니다. 지금 위험은 없으니 대표님이 시점을 정합니다** |
| `MN-22-6` | #22 | id 만 알면 로그인 없이 처방 내용을 읽습니다. 지금은 0 행이라 노출은 없습니다 | **처방 공유를 할지 말지 — 대표님 결정입니다** |
| `MN-21-1` | #21 | 결정 대기 47건이 줄지 않고, 리서치 품질을 재는 사람 검사가 0건입니다 | **예약 작업은 대표님 확인 뒤에 고칩니다** |
| `MN-20-2` | #20 | Android 가 file:// WebView 라 블루투스·OAuth·푸시가 구조적으로 안 됩니다 | **배포 방식 결정 — 대표님. 웹앱 주소가 확정돼야 하고(MN-12-4 와 같은 물음), 설치형을 계속 나눠 줄지부터** |
| `MN-20-3` | #20 | iOS 는 WKWebView 라 블루투스·서비스워커가 없고, file:// 이라 OAuth 도 돌아오지 못합니다 | **MN-14-2 (HealthKit 을 붙일지) 와 iOS 배포 여부 — 대표님. 별도 앱이 되는 결정입니다** |
| `MN-20-4` | #20 | 설치형 앱 안에서 「Android Chrome에서 열어 주세요」·「배포한 https 주소에서 열어 주세요」가 떠서 막다른 길입니다 | **손님이 보는 문구 변경 (R16) 과 배포 주소 확정 — 대표님** |
| `MN-18-2` | #18 | 혈압·체온 특성의 Time Stamp 필드(기기 시계)를 읽지 않아, 메모리 동기화 값의 잰 시각을 알 수 없습니다 | **실기기 확인이 필요합니다 — 어느 혈압계·체온계로 시험할지 대표님이 정해 주셔야 합니다. bluetooth.com 원문도 아직 대조 못 함** |
| `MN-18-3` | #18 | 관측에 구간(시작·끝)과 시간대 오프셋, 시계 출처가 없어 수면·걸음을 심박과 같은 축에 놓을 수 없습니다 | **건강 데이터 저장 형식 결정입니다 — 대표님. 지금 measurements · lifelogs 는 0건이라 옮길 데이터는 없습니다** |
| `MN-18-4` | #18 | 걸음은 합계로, 심박은 값 목록으로만 돌려줘 시각이 사라집니다 | **MN-14-2 (Health Connect·HealthKit 을 붙일지) 가 먼저입니다** |
| `MN-17-3` | #17 | 나머지 57종 색 — 중립색(#5A716D · #122423 · #8FA6A2 · #F7FCFB …)이 APP-STYLE 3-1 의 --n-* 단계와 값이 다릅니다 | **색이 실제로 바뀝니다 — 대표님 확인이 먼저입니다 (R16)** |
| `MN-17-4` | #17 | 모서리 13종. 기준은 --r-xs 8 · --r-sm 12 · --r-md 16 · --r-lg 20 · --r-full 5단계 | **눈에 띄는 변경 (R16)** |
| `MN-17-5` | #17 | 17px 이하에 font-weight:800 이 33곳. VaccineNote-Park#40 이 「800 은 28px 이상 제목만」으로 정했습니다 | **VaccineNote-Park#40 VN-40-2 와 같은 결정입니다. 대표님이 굵기 규칙을 확정하시면 같이 갑니다 (R16)** |
| `MN-17-6` | #17 | 「Sign in with Google and we'll read…」 안내 문장이 11px 입니다. 한국어 안내는 #11 에서 15px 로 올렸는데 영문 쪽은 남았습니다 | **눈에 띄는 문구 크기 변경 (R16). 다만 #11 에서 한국어에 이미 승인된 것과 같은 규칙입니다** |
| `MN-16-4` | #16 | 데모 일정에 「2026.10 예정 · 알림 설정됨」이 적혀 있는데 실제로 가는 알림이 없습니다 | **손님이 보는 문구 변경이라 대표님 확인이 먼저입니다 (UX-RULES R16)** |
| `MN-16-5` | #16 | 구독은 받는데 밀어 주는 쪽의 소스가 저장소에 없습니다. 배포돼 있는지도 알 수 없습니다 | **배포 현황을 대표님이 콘솔에서 확인해 주셔야 합니다. 함수 소스가 저장소에 없습니다** |
| `MN-16-6` | #16 | 복약 시간 알림이 없습니다. 복약 관련 코드는 QR 약봉투 기록 읽기뿐입니다 | **만들지 말지·어느 길로 갈지 대표님이 정해 주셔야 합니다. 앱 구조가 걸린 결정입니다** |
| `MN-15-1` | #15 | hwTipGoogle 이 안 도는 기능(Health Connect)을 설명하면서 저장 위치도 틀립니다. 식약처는 코드가 아니라 표시된 사용목적으로 의료기기를 가르므로, 이 문구 자체가 규제 판단의 근거가 됩니다 | **MN-14-1 과 같은 결정입니다 — Health Connect 를 붙일지, 그리고 R16 문구 확인** |
| `MN-15-2` | #15 | 「유전·의료 정보는 … 기기 안에만 저장되며」의 범위가 모호합니다. 약물유전형에 한정한 문장으로 읽히고 그건 맞지만, 질환(conditions)은 health_profiles 표로 서버에 올라갑니다(787줄) | **사용자에게 보이는 동의 관련 문구라 대표님 확인이 먼저입니다 (R16)** |
| `MN-15-3` | #15 | 「부작용 모니터링」·「위험 알람」·「진단」·「판정」·「비침습 연속 측정」·「MARD 가 낮아 정확하다」는 한 줄만 써도 의료기기로 판정될 수 있습니다. 지침서 원문이 사용목적은 표시·광고로 판단한다고 명시합니다 | **스토어 설명 문안이 아직 없습니다. 대표님이 초안을 주시거나 제가 쓰도록 말씀해 주셔야 합니다** |
| `MN-15-4` | #15 | 출시 전 「의료기기 해당여부 검토」를 신청해 두지 않으면, 나중에 의료기기로 판정될 때 스토어에서 내려야 합니다 | **신청할지 대표님이 정하셔야 합니다. 코드가 아니라 절차라 Claude Code 가 대신할 수 없습니다** |
| `MN-14-1` | #14 | 「Google 건강기기 연결 / Health Connect·Wear OS 센서 정보 공유」라고 적혀 있는데 실제로 도는 것은 블루투스(BLE) 직접 읽기입니다. 305줄은 안 도는 기능을 설명하면서 「정보는 기기 안에 저장돼요」라고 하는데 실제 블루투스 값은 클라우드로 갑니다 | **눈에 띄는 문구 변경이라 대표님 확인이 먼저입니다 (UX-RULES R16). 그리고 MN-14-2 와 한 몸입니다 — Health Connect 를 붙일지가 정해져야 방향이 정해집니다** |
| `MN-14-2` | #14 | HealthConnectManager.kt 와 HealthKitManager.swift 가 잘 짜여 있는데 어느 화면에서도 불리지 않습니다. 자바스크립트 다리가 없어 앱 안의 웹 화면이 부를 방법 자체가 없습니다 | **Health Connect·HealthKit 을 실제로 붙일지 대표님이 정하셔야 합니다. 안 붙이면 이 항목은 사라지고 MN-14-1 이 「이름표를 블루투스에 맞추기」가 됩니다** |
| `MN-14-4` | #14 | 개인정보처리방침이 저장소에 없었습니다. 건강 데이터를 읽는 앱은 두 스토어 모두 방침 URL 을 요구합니다 | **개인정보 보호책임자 이름과 연락처가 비어 있습니다. 대표님이 알려주셔야 채웁니다** |
| `MN-12-4` | #12 | 살아 있는 빌드 경로인지 7월 보관본인지 불분명합니다 | **살아 있는 경로인지 대표님이 알려주셔야 합니다** |

## 이미 반영된 것

| id | 이슈 | 무엇 | 커밋 |
|---|---|---|---|
| `MN-22-1` | #22 | 건강 데이터가 나가는 길이 둘로 적혀 있는데 실제로는 다섯입니다 (증상 기록 → hub_state, AI 도우미 → Anthropic, Google 생년월일) | `8d37434` |
| `MN-20-1` | #20 | 설치형 앱에서 블루투스·Google 로그인·푸시가 안 된다는 사실이 어디에도 없어, APK 를 나눠 주는 쪽이 모릅니다 | `d075251 · 8f7fee1` |
| `MN-17-1` | #17 | :root 토큰이 하나도 없어 브랜드 색이 61종 값으로 직접 박혀 있습니다 | `e4146ac` |
| `MN-17-2` | #17 | button·input·select·textarea 가 서체를 물려받지 않아 Arial 로 뜹니다. 로그인 단추 4개가 그렇습니다 | `e4146ac` |
| `MN-16-1` | #16 | 지원 여부 검사가 in 연산자만 써서 file:// 를 못 잡습니다. API 는 있고 등록만 거부됩니다 | `536f806` |
| `MN-16-2` | #16 | navigator.serviceWorker.ready 는 등록이 불가능하면 거부되지 않고 영원히 멈춥니다. VAPID 키를 채우는 순간 단추가 먹통이 됩니다 | `536f806` |
| `MN-16-3` | #16 | MediNote.sw.js 가 패키징 폴더에 복사되지 않습니다. 등록 코드는 들어 있는데 파일이 없습니다 | `536f806` |
| `MN-14-3` | #14 | 빌드가 세 군데서 막혀 있었습니다 — Gradle 래퍼 없음 · res/mipmap-* 없음 · health-connect 의존성 없음 | `ad2222c` |
| `MN-12-1` | #12 | 패키징 사본 셋이 웹앱(MediNote_app.html)과 두 달 넘게 갈라져 로그인·클라우드·증상기록 시트가 통째로 없습니다 | `393da5b` |
| `MN-12-2` | #12 | APK_빌드안내.md 가 있다고 적어 둔 워크플로가 저장소에 없어, Actions 탭에서 찾아도 아무것도 없습니다 | `ad2222c` |
| `MN-12-3` | #12 | 웹앱→패키징 복사를 손으로 하면 또 갈라집니다 | `ad2222c` |

---

반영한 뒤에는 **이슈의 `fix` 칸에서 `상태` 를 `반영됨 · <커밋>` 으로 바꾸고**,
이 파일을 `--report` 로 다시 만들어 주세요. 그래야 다음에 또 고치지 않습니다.
