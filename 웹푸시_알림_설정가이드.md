# MediNote 접종 알림(웹푸시) 설정 가이드 + Health Connect 계획

작성: 2026-07-21 · 대상: MediNote_app.html + MediNote.sw.js + send-push Edge Function

이미 붙어 있는 것 (이번 작업)
- 앱: "접종 시기 알림"을 누르면 **알림 권한 요청 → 푸시 구독 → Supabase에 구독 저장** (UI 변경 없음)
- 서비스워커(MediNote.sw.js): **푸시 수신 시 알림 표시**(`push`·`notificationclick`)
- Edge Function(send-push): 저장된 구독 전체에 **알림 발송** + 만료 구독 자동 정리

아래 5단계만 하면 실제로 발송됩니다. (전부 무료)

---

## 1) 구독 저장 테이블 만들기
Supabase 대시보드 → SQL Editor 에 실행:
```sql
create table if not exists public.push_subscriptions (
  endpoint   text primary key,
  sub        jsonb not null,
  created_at timestamptz default now()
);
alter table public.push_subscriptions enable row level security;
-- 앱(anon)이 구독을 저장할 수 있게 insert 허용 (읽기/발송은 서버가 service_role로 처리)
create policy "anon insert sub" on public.push_subscriptions
  for insert with check (true);
```

## 2) VAPID 키 발급 (푸시 서명용, 무료)
로컬 터미널에서:
```bash
npx web-push generate-vapid-keys
```
출력된 **Public Key / Private Key** 두 개를 복사합니다.

## 3) 앱에 공개키 넣기
`MediNote_app.html` 안에서 아래 줄을 찾아 **Public Key**를 붙여넣습니다:
```js
var VAPID_PUBLIC="";   // ← 여기에 공개키(Bben...) 붙여넣기
```
(비워두면 "알림 권한만" 켜지고, 실제 발송은 안 됩니다.)

## 4) Edge Function 배포 + 시크릿 등록
```bash
# 함수 배포 (send-push 폴더를 supabase/functions/send-push 로 두고)
supabase functions deploy send-push

# 시크릿 등록 (VAPID 키 — SUPABASE_URL/SERVICE_ROLE_KEY는 자동 제공됨)
supabase secrets set VAPID_PUBLIC="<공개키>" VAPID_PRIVATE="<비공개키>"
```
> 콘솔에서도 가능: Edge Functions → send-push 배포, Project Settings → Edge Functions → Secrets 에 VAPID_PUBLIC·VAPID_PRIVATE 추가.

## 5) 테스트 발송
```bash
curl -X POST "https://<프로젝트ref>.functions.supabase.co/send-push" \
  -H "Authorization: Bearer <anon 키>" -H "Content-Type: application/json" \
  -d '{"title":"MediNote","body":"독감 접종 시기예요. 예정일 3일 전입니다."}'
```
앱에서 "접종 시기 알림"을 한 번 눌러 구독한 기기라면, 폰에 알림이 뜹니다.

---

## 자동 발송(스케줄) — 예정일 2주 전·3일 전
Supabase에서 `pg_cron` + `pg_net` 확장을 켠 뒤, 매일 아침 함수를 부르게 합니다:
```sql
-- 확장 활성화 (Database → Extensions 에서 pg_cron, pg_net 켜기)
select cron.schedule(
  'medinote-daily-push', '0 9 * * *',   -- 매일 09:00 UTC (한국 18:00) — 원하는 시간으로
  $$ select net.http_post(
       url    := 'https://<프로젝트ref>.functions.supabase.co/send-push',
       headers:= jsonb_build_object('Content-Type','application/json','Authorization','Bearer <anon 키>'),
       body   := jsonb_build_object('title','MediNote','body','접종 예정일이 다가와요. 앱에서 확인해 주세요.')
     ); $$
);
```
> "누구에게 2주 전·3일 전"처럼 사람별 정확한 타이밍은, 각 사용자의 **접종 예정일이 DB에 저장**돼 있어야 계산할 수 있습니다. 지금 원본 앱은 예정일을 메모리에만 두므로, 이 정밀 타이밍은 접종 데이터를 DB에 저장하도록 연결(React 소스 필요)한 뒤 함수에서 `WHERE 예정일 - now() IN (14일, 3일)`로 대상만 골라 보내면 됩니다. 현재 함수는 "구독한 전원에게" 보내는 형태입니다.

---

## Health Connect · Wear OS (네이티브 건강데이터) — 정확한 현실과 계획
- **웹(PWA)에서는 Health Connect를 직접 못 읽습니다.** 표준 블루투스(BLE) 기기는 앱의 "건강기기 연결"에서 이미 실제로 읽지만(Web Bluetooth), Health Connect는 안드로이드 OS 권한이라 JS에서 접근 불가입니다.
- 방법은 앱을 **네이티브로 감싸는 것**입니다. 두 경로:
  1. **Capacitor 래핑**(권장): 이 HTML을 Capacitor 앱으로 감싸고 `@capacitor-community/health-connect` 류 플러그인을 붙이면, JS에서 `HealthConnect.read(...)`로 걸음수·심박·혈압 등을 읽어 앱/Supabase로 넘길 수 있습니다. Play스토어 배포도 여기서 함께 처리됩니다.
  2. **순수 TWA**(Bubblewrap): 설치·오프라인엔 좋지만 TWA는 JS↔네이티브 브리지가 없어 Health Connect 접근이 안 됩니다. → Health Connect가 필요하면 1번(Capacitor)로 가야 합니다.
- 권장: 지금은 **Web Bluetooth(이미 동작)** 로 실기기 읽기를 쓰고, Health Connect가 꼭 필요해지면 Capacitor 래핑으로 확장. 이건 안드로이드 빌드 환경(Android Studio)이 필요해서, 진행하실 때 단계별로 붙여 드리겠습니다.

---

## 배포 파일 체크리스트 (웹 링크 사용)
같은 폴더에: `MediNote_app.html`, `MediNote.sw.js`, `manifest.webmanifest`, `icon-192/512/maskable.png`, `apple-touch-icon.png`, `favicon-32.png`, 로고 PNG.
HTTPS(GitHub Pages 등)에 올리면 링크로 바로 사용 — 알림·블루투스는 Android Chrome에서 동작(iOS Safari는 블루투스 미지원).
